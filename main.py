import pytz
import sys

print(pytz.all_timezones)
print(sys.path)

# ! When you bumped into an error about modules not found...
# * Make sure that you are using the `python` inside the virtual environment and not the global one
